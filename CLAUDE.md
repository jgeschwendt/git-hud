# Instructions

## Basic Memory

**Session start**: `get_current_project` → if not `git-hud`, use `switch_project`.

**Before work**: `search_notes` for related patterns/decisions.

**After features**: `write_note` to capture learnings, decisions, non-obvious insights.

## Standards

- Expert-level context, minimal tokens
- Edit over create
- Use tools, not training data
